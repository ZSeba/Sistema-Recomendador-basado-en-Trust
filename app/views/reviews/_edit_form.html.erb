<% if @review == nil %>
<% @review = review  %>
<% end %>
<%= form_for(@review) do |f| %>
  <% if @review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% @review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    Stars
  <input type="hidden" id="input-id"/>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <% #binding.pry %>
  <div id="review_id", style="display:none;"> <%=@review.id%> </div>
  <div class="field" style="display: none;">
    <%= f.text_field :to_item, value: @review.to_node.name,type:"hidden" %>
  </div>
    <div class="field" style="display: none;">
      <%= f.text_field :from_user, value: @review.from_node.name,type:"hidden" %>
    </div>
  <div class="actions">
    <%= f.submit class: 'btn btn-default'  %>
  </div>
<% end %>
